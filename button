<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>New Story Editor</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            margin: 20px;
            padding: 20px;
            background-color: #f8f8f8;
            color: #333;
        }

        h1 {
            color: #333;
        }

        textarea {
            width: 100%;
            height: 300px;
            margin: 10px 0;
            padding: 10px;
            resize: none;
        }

        button {
            padding: 10px 20px;
            font-size: 16px;
            background-color: #007BFF;
            color: #fff;
            border: none;
            cursor: pointer;
        }

        button:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>

    <h1>Create a New Story</h1>

    <textarea id="story-editor" placeholder="Start writing your new story here..."></textarea>

    <button onclick="saveStory()">Save Story</button>

    <script>
        function saveStory() {
            // Get the content from the text area
            var storyContent = document.getElementById('story-editor').value;

            // You can perform actions to save the story, e.g., send it to a server
            // For demonstration purposes, log the content to the console
            console.log(storyContent);

            // Optionally, redirect the user to the main stories page after saving
            window.location.href = "index.html";
        }
    </script>

</body>
</html>
